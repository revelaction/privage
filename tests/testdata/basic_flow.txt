# Smoke test for version
exec privage version
stdout 'privage version'

# Test init
exec privage init
stderr 'Generated age key file'
exists .privage.conf
exists privage-key.txt
exists .gitignore

# Test status
exec privage status
stdout 'Found age key file'
stdout 'Found config file'
